# backend/nixpacks.toml

[phases.setup]
nixPkgs = ["zlib", "jdk17"]

[phases.build]
cmds = ["chmod +x gradlew", "./gradlew clean build -x check -x test -Pproduction"]

[start]
cmd = "java -Xmx350m -Xss512k -jar build/libs/app.jar"